package deduction;

import java.lang.Number;

rule "testRule"
	dialect "mvel"
	when
		>emp : Employee( salary >= 1000 , salary < 2000 )
	then
		>emp.setGrade( "Grade" + emp.getSalary().toString()  );
end
