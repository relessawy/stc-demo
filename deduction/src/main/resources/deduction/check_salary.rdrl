package deduction;

import java.lang.Number;

rule "check_salary"
	dialect "mvel"
	ruleflow-group "salary"
	when
		emp : Employee( )
		vac : Vacation( hours <= ( emp.getSalary() ) )
		request : DeductionRequest( )
	then
		request.setRequestStatus( "Approved" );
end
